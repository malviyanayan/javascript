Summery -----

var:  Function-scoped, hoisted, can be re-declared.
let:  Block-scoped, temporal dead zone, cannot be re-declared.
const:  Block-scoped, temporal dead zone, cannot be re-declared or reassigned, but objects are mutable.