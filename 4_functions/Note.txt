A function declaration is a way to define a named function in JavaScript.
It starts with the function keyword, followed by the name of the function, a
    list of parameters (enclosed in parentheses), and the function body (enclosed in curly braces).